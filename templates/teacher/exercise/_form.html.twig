{{ form_start(exerciseForm) }}
<div class="form-group mb-3">
    {{ form_row(exerciseForm.exerciseName) }}
</div>
<div class="form-group mb-3">
    {{ form_row(exerciseForm.exerciseContent) }}
</div>
<div class="form-group content-type-input mb-3" id="lecture-text">
    {{ form_row(exerciseForm.state) }}
</div>
<div class="form-group content-type-input mb-3" id="lecture-text">
    {{ form_row(exerciseForm.closeDate) }}
</div>
<div class="form-group text-right">
    <button class="btn btn-success" type="submit">{{ 'app.save' | trans }}</button>
</div>
{{ form_end(exerciseForm) }}
